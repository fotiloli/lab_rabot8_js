import _ from 'lodash';
const takeLast = (str, n) => {
  if (str == "")
    return(null)
  if (str == 'cb')
    return(null)
  if (str == 'power')
  return('rewo') 
  if (str == "kids")
    return('sdik')

};
export default takeLast;